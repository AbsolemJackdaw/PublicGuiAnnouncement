modLoader="javafml" #mandatory
license="GNU V3"
loaderVersion="[44,)"
issueTrackerURL="https://github.com/ArtixAllMighty/PublicGuiAnnouncement/issues"
logoFile="logo.png" #optional

[[mods]] #mandatory
modId="publicguiannouncement" #mandatory
version="${file.jarVersion}" #mandatory
displayName="Public GUI Announcement" #mandatory
credits="Thanks to StrickerRocker for fabric support, Comeupper for the idea" #optional
authors="Subaraki" #optional
description='''

Shows a simple representation of the screen players are currently looking at

'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.publicguiannouncement]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[44.1.0,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.publicguiannouncement]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.19.3,)"
    ordering="NONE"
    side="BOTH"